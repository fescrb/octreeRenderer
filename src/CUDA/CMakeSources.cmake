set(this_sources    ${this_sources}
                    CUDAContext.cpp
                    CUDADevice.cu )

set(this_headers    ${this_headers}
                    CUDAContext.h
                    CUDADevice.h )

#set(shaders         RayTracing.cl)
